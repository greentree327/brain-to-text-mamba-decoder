# Core Dependencies
# NOTE: Install PyTorch via conda for GPU support:
#   conda install pytorch pytorch-cuda=12.1 -c pytorch -c nvidia
# Or use CPU version:
#   pip install torch
torch>=2.1.0
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# NOTE: Install mamba-ssm via conda to avoid Windows compilation:
#   conda install -c conda-forge mamba-ssm causal-conv1d
# DO NOT pip install mamba-ssm on Windows (compilation fails)

# NLP & Metrics
editdistance>=0.8.1,<1.0.0
h5py>=3.8.0

# Kaggle & HuggingFace
kagglehub>=0.1.0
huggingface-hub>=0.16.0,<1.0.0
transformers>=4.30.0,<5.0.0

# Testing & Profiling
pytest>=7.4.0,<8.0.0
pytest-cov>=4.1.0,<5.0.0
psutil>=5.9.0,<6.0.0

# Development & Notebooks
jupyter>=1.0.0,<2.0.0
ipython>=8.10.0,<9.0.0
matplotlib>=3.7.0,<4.0.0
scikit-learn>=1.3.0,<1.4.0

# Optional: Language Models
# kenlm>=2.10.0  # Uncomment if using KenLM
# ctc-decoders>=0.0.15  # For CTC beam search

# Code Quality
black>=23.0.0,<24.0.0
flake8>=6.0.0,<7.0.0
mypy>=1.4.0,<1.5.0
